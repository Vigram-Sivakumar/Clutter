import { Globe as PhosphorGlobe, type IconProps } from '@phosphor-icons/react';
import { ICON_WEIGHT } from '../tokens/icons';

export const Globe = (props: IconProps) => {
  return <PhosphorGlobe weight={ICON_WEIGHT} {...props} />;
};























