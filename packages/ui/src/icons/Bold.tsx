import { TextB as PhosphorBold, type IconProps } from '@phosphor-icons/react';
// import { ICON_WEIGHT } from '../tokens/icons';

export const Bold = (props: IconProps) => {
  return <PhosphorBold weight='bold' {...props} />;
};

