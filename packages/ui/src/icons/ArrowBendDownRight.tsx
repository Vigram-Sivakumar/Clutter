import { ArrowBendDownRight as PhosphorArrowBendDownRight, type IconProps } from '@phosphor-icons/react';
import { ICON_WEIGHT } from '../tokens/icons';

export const ArrowBendDownRight = (props: IconProps) => {
  return <PhosphorArrowBendDownRight weight={ICON_WEIGHT} {...props} />;
};

